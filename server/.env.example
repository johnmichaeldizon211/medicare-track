DATABASE_URL="mysql://root:root@localhost:3307/medicare_track"
PORT=4000
NODE_ENV=development
JWT_ACCESS_SECRET="change-me-access-secret"
JWT_REFRESH_SECRET="change-me-refresh-secret"
JWT_ACCESS_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"
APP_ORIGIN="http://localhost:3000"
# Optional: one-line Firebase service account JSON or a path relative to server/.
# Alternatively set GOOGLE_APPLICATION_CREDENTIALS to a Firebase service account file.
FIREBASE_SERVICE_ACCOUNT_JSON=
